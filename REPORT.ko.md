# Codex 하네스 조사 결과

OpenAI가 실제로 무엇을 열었는지를 발표문이 아니라 소스에서 읽었습니다. 그리고 그것이 자체 하네스를
만드는 데 어떤 의미인지 정리했습니다.

> 저장소의 문서 본문은 영어입니다. 영문 보고서는 [REPORT.md](REPORT.md)에 있습니다.

| | |
|---|---|
| 조사 기간 | 2026-09-14 → 09-15 |
| 1차 출처 | `openai/codex` 리포지터리, `openai/openai-openapi`, 공식 문서 |
| 산출물 | 문서 17개, 약 5,000줄 |
| 웹 버전 | <https://claude.ai/artifact/NC1DXYvqdUp1J1sSeKH9bh> |

> **범위 안내 (2026-09-23).** 이 문서는 **Codex 조사의 보고서**입니다. 저장소는 이후 *에이전트
> 하네스 일반*으로 범위가 넓어졌고, 두 번째 조사를 담고 있습니다 —
> [`browser-agents/`](browser-agents/README.md), 브라우저형 에이전트(Aside, Comet, Dia, Neon,
> Browser Use)를 다룹니다. 둘을 가로지르는 종합은 **[`SYNTHESIS.md`](SYNTHESIS.md)** 이고,
> 거기서 실행 표면에 의존하는 설계 축과 그렇지 않은 축을 가릅니다.
>
> 아래 내용 중 그 조사로 무효가 된 것은 없습니다. **몇몇 결론은 외부 증거를 얻었고**,
> [§ 외부 증거](#외부-증거--브라우저-조사가-확인해준-것)에 모았습니다.
>
> **추가 (2026-09-26).** 세 번째 조사 [`strands/`](strands/README.md)는 AWS의 Strands Agents SDK와
> Strands harness를 읽었습니다. 와이어 너머에서 서비스되는 것이 아니라 호출자 프로세스 안에 링크되는
> 하네스입니다. 이 조사로도 무효가 된 것은 없고, **두 결론에 단서가 붙었으며** 여러 결론이 반대편에서
> 확인됐습니다 — [§ 세 번째 사례](#세-번째-사례--strands가-시험한-것)에 모았습니다.

## 요약 — 요구사항 하나가 플랫폼과 충돌합니다

Codex는 `wire_api = "chat"`을 제거했습니다. `WireApi` enum의 변형은 `Responses` 하나뿐이고, 옛 값을
쓰면 토론 스레드를 가리키는 의도적인 역직렬화 에러가 발생합니다.
**Chat Completions 지원은 `codex app-server`를 그대로 감싸서는 얻을 수 없습니다.**

이 사실 하나가 선택지의 순서를 바꿉니다. 멀티 프로바이더, 마켓플레이스, Windows 샌드박스, 제품 표면
전체는 Codex가 공개한 것으로 충분히 감당됩니다. 와이어 프로토콜만이 플랫폼이 아니라고 답하는
지점입니다.

## 공개 경과

하네스는 모델과 과업 사이에 있는 실행 시스템입니다 — 스레드 수명주기와 영속화, 설정과 인증, 샌드박스
안의 도구 실행. OpenAI는 이것을 세 층으로 열었습니다. 오픈소스 바이너리와 프로토콜, 언어별 SDK,
그리고 자신들이 운영하는 관리형 API입니다.

| 날짜 | 공개 | 의미 |
|---|---|---|
| 2026-02-04 | Unlocking the Codex harness | App Server 아키텍처와 JSON-RPC 프로토콜 설계 공개 |
| 2026-02-11 | Harness engineering | 내부 "수기 코드 0줄" 실험 공개, 하네스 작업을 하나의 직무로 명명 |
| 2026-08-19 | Codex as a platform | CLI · app-server · SDK를 Apache-2.0 오픈 에이전트 하네스로 포지셔닝 |
| 2026-09-10 | Agents API 퍼블릭 베타 | 같은 하네스를 OpenAI가 운영하는 관리형 서비스로 제공 |

## 요구사항 평가

| 요구사항 | 판정 | 내용 |
|---|---|---|
| **제품 표면 전체** | 충분히 제공됨 | 클라이언트 메서드 104개, 서버 요청 10개, 알림 84개. 에이전트 루프는 20개 남짓이고 배포 가능한 최소선은 45개 정도 |
| **마켓플레이스** | 새로 만들지 말 것 | `agent-plugins.org`에 벤더 중립 스키마가 존재하고 Codex는 Claude 호환 매니페스트도 수용. 소스 4종, 버전별 설치 캐시, 항목별 설치 정책 |
| **Windows 샌드박스** | 보기보다 무거움 | `elevated` / `unelevated` 두 모드. 강한 쪽은 특권 Windows 서비스를 요구 — 설치 관리자와 수명주기 문제 |
| **서드파티 모델** | 1급 설계 | 프로바이더가 데이터로 모델링됨. 내장 5개는 의도적이고 나머지는 `model_providers`로 추가. command 기반 토큰 발급이 독자 인증을 흡수 |
| **Chat Completions** | **막힘** | 제거됨. `ollama-chat`도 함께 폐기. 설정으로 되살릴 방법 없음 |

## 어댑터 타당성

스펙끼리 비교하지 않고 Codex가 실제로 구성하는 페이로드를 읽었습니다. 그 결과 결론이 두 번, 양쪽
방향으로 바뀌었습니다.

### 문제를 다루기 쉽게 만드는 상수들

```
store: false,          // 매 턴 전체 히스토리 재전송
stream: true,          // 비스트리밍 경로 생략 가능
tool_choice: "auto",   // required/named-tool 매핑 불필요
include: ["reasoning.encrypted_content"]   // 하드코딩
```

Codex는 와이어상 완전히 무상태이므로 어댑터에 **세션 저장소도, response-id 레지스트리도, 만료
처리도 필요 없습니다** — 보통 Responses API 에뮬레이션에서 가장 어려운 부분입니다.

### 통하는 지점과 깨지는 지점

| 항목 | 결과 | 내용 |
|---|---|---|
| 요청 필드 | 17 중 13 | `prompt_cache_key` 포함, 직접 또는 수용 가능한 대응 |
| 세션 상태 | 불필요 | 무상태 어댑터, 수평 확장이 공짜 |
| 스트리밍 이벤트 | 7개면 충분 | 25종 인식하나 실제 처리는 12종. 툴 호출 인자 델타는 아예 무시 |
| 입력 아이템 | 18 중 8 | 4종 폐기 가능, 5종은 Responses 전용 서버사이드 도구 |
| 도구 변환 | 문제 2건 | 네임스페이스는 양방향 이름 재작성 필요, freeform 도구는 문법 제약 상실 |
| 추론 유지 | **불가능** | Chat Completions에는 추론 아이템도, 되돌려보낼 자리도 없음 |

### 추론이 유일하게 중요한 이유

`store`가 false이기 때문에 Codex는 모델 자신의 이전 추론을 `encrypted_content`에 담아 매 턴
되돌려보내야 합니다. 무상태 클라이언트가 추론 체인을 살려두는 방식이 바로 이것입니다.
Chat Completions에는 그것을 넣을 자리가 없습니다.

비용은 OpenAI 자신의 표현으로 구체화됩니다. ARC-AGI-3에서 추론 유지와 컨텍스트 컴팩션이 GPT-5.6
Sol을 13.3%에서 38.3%로 끌어올리면서 출력 토큰은 1/6로 줄였습니다. 모델 변경이 아니라 하네스 설정
두 개입니다. **손실은 대상 모델이 얼마나 추론하느냐에 비례합니다** — 비추론 서드파티 모델이라면
잃을 체인 자체가 없습니다.

## 검증 기록

2차 출처에서 가져온 주장은 전부 리포지터리 또는 OpenAPI 스펙과 다시 대조했습니다. 널리 인용되는
두 건이 틀린 것으로 드러났고, 모순처럼 보이던 하나는 애초에 모순이 아니었습니다.

| 주장 | 판정 | 근거 |
|---|---|---|
| JSON-RPC `-32001`, "Server overloaded; retry later.", 큐 용량 128 | **확인** | 전송 계층에 문자열과 `CHANNEL_CAPACITY = 128`이 그대로 존재 |
| WebSocket이 `Origin` 헤더를 가진 요청을 거부 | **확인** | websocket 전송에 전용 CSRF 미들웨어 존재 |
| ARC-AGI-3: 13.3% → 38.3%, 출력 토큰 1/6 | **1차 승격** | OpenAI 자체 포스트에 그대로 기술, 전용 글까지 링크 |
| WebSocket 기본 리스너가 `127.0.0.1:9090` | **반증** | 기본 전송은 `stdio://`이며 WS 포트는 명시 필수 |
| 스레드가 30분 유휴 후 언로드 | **반증** | `thread_unload_delay_secs` 기본값은 **60초**, 구독자 없음 *과* 비활성 둘 다 필요 |
| `initialize` 응답에 `serverInfo` / `capabilities` | **반증** | 생성 스키마의 필드는 넷 — user agent, Codex home, 플랫폼 계열, OS |
| 플랫폼 포스트 게시일 8/19 vs 8/20 | **해소** | Archive 최초 캡처가 2026-08-19 21:07 UTC. 양쪽 다 자기 시간대로는 정확 |
| 9개짜리 샌드박스 프로바이더 목록 둘이 모순 | **해소** | 서로 다른 제품. 7개 공통, DigitalOcean·OCI는 API 전용, Unix-local·Docker는 SDK 전용 |
| Windows 샌드박스 내부 (ACL, WFP, 토큰 제한) | **추론** | 산문 문서가 아니라 소스 트리 모듈명에서 읽음. 문서 전반에 명시 |

## 외부 증거 — 브라우저 조사가 확인해준 것

Codex 조사는 한 벤더의 리포지터리를 읽은 것이었습니다. 이후 브라우저형 에이전트를 조사하면서
다른 사람들이 만든 제품을 들여다봤고, 아래 권고 중 여럿이 **서드파티 시스템에서 독립적으로 구현된
것**으로 확인됐습니다. 내부 정합성보다 강한 증거입니다.

가장 뚜렷한 사례는 **Aside** 입니다. Chromium 포크 AI 브라우저이고, 데몬 바이너리를 추출해
읽었습니다 ([`browser-agents/09`](browser-agents/09-aside-browser-internals.md)):

```js
CODEX_TOOL_CALL_PROVIDERS = new Set([`openai`, `openai-codex`, `opencode`])
// 인접: supportsAdditionalTools, supportsToolSearch, supportsMidConvoSystemMessages
```

| 이 보고서의 결론 | 브라우저 조사가 보여준 것 |
|---|---|
| **wire protocol 경계가 첫 결정이다** (권고 1) | 서드파티 하네스에 `openai-codex` 가 **모델 프로바이더 id 로 등록**돼 있습니다. 이 경계는 이론이 아니라 다른 사람들이 실제로 건너는 지점입니다 |
| **`responses_lite` 는 어댑터가 처리해야 할 두 번째 요청 형태다** ([docs/16 §10.2](docs/16-responses-chat-adapter.md)) | 같은 데몬이 **`supportsAdditionalTools` capability 플래그**를 갖고 있습니다. 서드파티 하네스가 Codex 의 두 요청 형태를 권고한 그대로 흡수했습니다 |
| **프로바이더를 코드 분기가 아니라 데이터로** (권고 2, 4) | Aside 는 **16종 이상의 프로바이더 id** 를 데이터로 갖고, 사용자 구독과 API 키를 받습니다. 다중 프로바이더는 가정이 아니라 실제 설계 지점입니다 |
| **유지된 추론을 프로바이더별 capability 로** (권고 3) | 그 데몬이 프로바이더별 차이를 표현하는 방식이 이미 capability 플래그입니다. 여기서 권고한 모양이 이미 쓰이고 있습니다 |
| **승인은 프로토콜 원시형이어야 한다** | 확인됐고, **확장**됐습니다 — Aside 의 승인 프롬프트는 버튼 배열과 번호 목록 텍스트 폴백을 함께 만듭니다. 채팅 채널에서 렌더되도록 설계했기 때문입니다. App Server 의 승인은 클라이언트 UI 를 전제합니다 |
| **권한 표현력** | Aside 의 정책 엔진은 **도구 glob + 인자별 eq/regex** 로 매칭합니다. `ExecPolicyAmendment` 의 모양을 넘어서므로 역으로 베낄 가치가 있습니다 |

반대로 브라우저 조사가 **확인해주지 못한** 것도 있습니다 — 유지된 추론입니다. 조사 범위의 브라우저
에이전트들은 그 계층을 노출하지 않습니다.

두 조사를 합친 구축 체크리스트는 [`SYNTHESIS.md` §5](SYNTHESIS.md)에 있습니다.

### 그리고 나중에 실측된 권고 하나

권고 7 — **채널이 렌더할 수 있는 승인 프롬프트** — 은 두 조사의 결론을 구현하고 그 게이트 주변의 주입
방어를 실제로 공격하면서([`SYNTHESIS.md` §6.4](SYNTHESIS.md)) 설계 취향의 문제이길 그만뒀습니다.
공격 16건 중 5건 관통 — 인지 계층 필터는 무너졌고 게이트는 버텼습니다.

> 📌 주입된 페이지 내용에 모델이 완전히 설득당한 상태를 가정해도 행동은 거부됐고 페이지는 바뀌지
> 않았습니다. **게이트는 이 스택에서 공격자가 연구할 수 있는 휴리스틱이 아닌 유일한 방어**입니다.
> 모델의 통제 루프 바깥에 있기 때문입니다. 승인을 UI 기능이 아니라 프로토콜 원시값으로 두라는
> 근거이고, 이제 추론이 아니라 실측입니다.
>
> ⚠️ 아래 Strands 조사로 단서가 붙었습니다: 루프 바깥에 있는 것은 **필요조건이지 충분조건이 아닙니다.**

## 세 번째 사례 — Strands가 시험한 것

Codex와 Aside는 둘 다 에이전트를 조종하는 쪽과 루프 사이에 경계를 둡니다. **Strands에는 그 경계가
없습니다** — "호스팅된 컨트롤 플레인 없이 당신의 프로세스 안에서 돕니다." 그래서 앞의 두 조사가 닿지
못한 각도에서 이 보고서의 권고를 시험합니다: 와이어가 아예 없으면 각 권고는 어떻게 되는가.
`strands-agents/harness-sdk` @ `15da9dc`(2026-09-25)의 소스와 설계 문서를 읽고, 승인 경로는 SDK 자체의
스크립트 목 모델로 프로브했습니다.

| 이 보고서의 결론 | Strands가 보여준 것 |
|---|---|
| **승인은 프로토콜 원시값이어야 한다** | 반대편에서 확인됐습니다. 와이어가 없으니 Strands의 승인은 예외와 재진입 관례입니다. 도구나 훅이 **처음부터 다시 실행**되므로 승인 호출 이전의 부수효과가 두 번 일어나고, 프로세스 밖에서는 답할 수 없습니다. A2A 클라이언트는 승인 응답을 아예 보내지 않습니다 |
| **루프 밖 게이트만이 버텼다** (권고 7, 실측) | **단서가 붙었습니다.** Strands는 바로 그런 게이트를 Cedar 정책 백엔드까지 갖춰 두었지만 **꺼진 채로** 출하합니다. 오류 경로를 프로브하니 오류 나는 Cedar `forbid`는 **허용**되고, steering 핸들러는 **참으로 평가되는 모든 답 — `"no"` 포함 — 에 승인**하며, 정책은 문서의 우선순위가 아니라 등록 순서로 합성되고, CLI는 모든 거부를 "묻기"로 낮춥니다. 위치는 여섯 속성 중 하나입니다 ([`SYNTHESIS.md` §2.7](SYNTHESIS.md)) |
| **프로바이더를 데이터로 모델링하라** (권고 2, 4) | **단서가 붙었습니다.** Strands는 의도적으로 반대를 택했습니다. 내부 와이어를 Bedrock ConverseStream 그대로 고정하고 벤더마다 변환 *클래스*를 둡니다. 벤더 고유 기능과 일급 Chat Completions를 얻고, 운영자 수준의 확장을 잃습니다. **프로바이더-데이터는 와이어가 공유될 때 성립합니다** — Codex에서는 공유되므로 그곳의 권고는 유효합니다 |
| **추론 유지를 프로바이더별 capability로** (권고 3) | 확인됐습니다. Strands의 OpenAI 경로는 Chat Completions **와** Responses 모두 턴 사이에 추론을 경고와 함께 버립니다. 벤더별 변환기 설계는 누군가 지키기로 정하지 않으면 추론을 잃고, 아무도 정하지 않았습니다 |
| **어댑터를 무상태로** (권고 2) | 확인됐습니다. Strands의 옵트인 stateful Responses 모드는 호출마다 로컬 이력을 비우고, 대화 매니저를 아예 거부하며, 미검증이지만 도구 루프 안에서 `previous_response_id`와 함께 호출분 항목을 다시 보내는 것으로 보입니다. 예측대로 상태는 자기 실패 모드를 데려왔습니다 |
| **포터블 플러그인 스키마를 채택하라** (권고 5) | 반대편에서 확인됐습니다. Strands 플러그인은 매니페스트 없는 `pip` 패키지라 정책 계층이 들여다보거나 거부할 것이 없고, 스킬의 `allowed-tools`는 강제되지 않습니다 |
| **도구 glob과 인자별 매처** (권고 8) | 반대편에서 확인됐습니다. Strands의 가장 세밀한 정책 단위는 도구 이름이고, `shell`을 "항상 허용"하면 모든 명령이 허용됩니다 |

확인이 아니라 새로 나온 발견도 둘 있고, 둘 다 아래 권고가 됐습니다.

- **하네스 자신의 자격증명이 모델의 도구에 닿습니다.** 프로바이더 키가 프로세스 환경변수에 있고,
  모든 셸 호출이 그것을 물려받습니다. 승인된 `env` 한 번이면 출력됩니다.
- **모델이 믿으라고 들은 권위 태그를 위조할 수 있습니다.** 하네스 프롬프트는 도구 결과 안의
  `<system-reminder>` 태그가 하네스에서 온다고 말하지만, 가져온 페이지·파일·MCP 출력에서 그 태그를
  이스케이프하는 곳이 없습니다. 소스로 읽었고 모델에 돌려보지는 않았습니다.

그리고 기록 하나: Strands의 벤치마크 헤드라인 — "비용 28% 절감, 정확도는 동등 이상" — 은 차트 자체
데이터로 같은 모델 19쌍 중 7쌍에서 반박됩니다. 분산이 보고되지 않았으므로 반박되는 것은 **문구**이지
순위가 아닙니다.

## 권고

1. **와이어 프로토콜 경계를 가장 먼저 정하십시오.** Codex 코어를 재사용할 수 있는지가 여기서
   갈립니다. 아키텍처의 나머지는 전부 이 결정의 하류에 있습니다.
2. **어댑터는 포크가 아니라 프로바이더 형태의 프록시로 지으십시오.** 지원되는 확장점에 꽂히고 코어를
   건드리지 않습니다. 무상태를 지키십시오 — response-id 저장소가 생기는 순간 이점이 사라집니다.
3. **추론 유지를 프로바이더별 capability로 만드십시오.** 프로바이더 구조체에 이미 그럴 형태가 있습니다.
4. **모델 카탈로그를 명시적으로 배포하십시오.** 요청 형태가 *최장 접두사 슬러그 매칭*으로 결정되는
   per-model 플래그에 달려 있고 config 오버라이드가 없습니다. 이름을 `gpt-6-astra-turbo`로 지으면
   다른 모델의 플래그를 조용히 물려받습니다.
5. **포터블 플러그인 스키마를 채택하십시오.** 포맷을 새로 만드는 대신 자기 확장을 네임스페이스에 넣으십시오.
6. **특권 Windows 서비스를 일찍 계획하십시오.** 강한 샌드박스 모드가 여기에 달려 있습니다.

브라우저 조사 이후 두 가지를 더합니다. 다른 사람들이 실제로 출하한 시스템에 근거합니다.

7. **승인 프롬프트를 채널이 렌더할 수 있는 모양으로 설계하세요.** 버튼 배열에 번호 목록 텍스트
   폴백을 더하고, 그냥 답장해도 된다는 힌트를 답니다. 원격·비동기 승인이 공짜로 가능해집니다.
   클라이언트 UI 를 전제하면 그 길이 막힙니다.
8. **권한 정책에 도구 glob 과 인자별 매처를 두세요.** "`bash` 허용"은 너무 거칩니다. "첫 인자가
   이 정규식에 맞는 `bash` 만 허용"이 실제로 쓸모 있는 수준이고, 서드파티가 이미 출하했습니다.

Strands 조사 이후 두 가지를 더합니다. 오류 경로를 프로브했을 때 실제로 일어난 일에 근거합니다.

9. **승인 게이트에 위치만이 아니라 여섯 속성을 모두 주십시오:** 기본으로 켜짐, 모든 오류 경로에서
   닫힘, 답을 열거형으로 파싱, 명시적 우선순위, 어떤 것도 낮출 수 없는 확정 거부, 그리고 *최종* 도구
   입력에 대한 인가. 각 오류 경로를 입력이 도달했음을 증명하는 대조군과 함께 시험하십시오 — Strands의
   설계 문서는 여섯 중 넷에 대해 올바른 동작을 서술하는데 코드는 다르게 합니다.
10. **하네스 자신의 자격증명을 모든 도구 프로세스의 환경에서 빼십시오.** 그리고 모든 도구 결과가
    권위 표지를 이스케이프하지 않는 한, 모델에게 그 표지가 도구 결과에 나타날 수 있다고 말하지 마십시오.

## 조사 방법

정정 대부분은 산문이 아니라 생성된 스키마와 Rust 소스를 읽어서 나왔습니다. 메서드 목록, 정확한 요청
페이로드, 에러 코드, 타임아웃 기본값은 전부 커밋된 아티팩트이고, 블로그와 3자 정리글은 그것으로부터
멀어집니다.

실용적인 발견 하나가 반복해서 값을 했습니다. **OpenAI 문서 URL 끝에 `.md`를 붙이면 원문 마크다운이
반환됩니다.** 손실 있는 페이지 요약이 코드 예제가 온전한 1차 텍스트로 바뀌었고, 존재조차 몰랐던
가이드 14개가 드러났습니다.

**이 수법은 일반화되지만 만능은 아닙니다.** 이후 다른 벤더에 적용한 결과: Aside ✅ (`/llms.txt`
색인과 `.md` 원본, 문서 16종), Opera ✅, **Dia ❌** (모든 경로가 동일한 클라이언트 렌더 셸),
**Strands ✅ 변형** (`/llms.txt`는 되지만 원문은 `<page>/index.md`에 있고, 지어낸 `<page>.md`는 404).
먼저 시도하되, URL을 직접 만들지 말고 색인이 주는 링크를 따르고, 실패하면 렌더된 페이지로 돌아가야 합니다.

브라우저 조사는 여기로 가져올 검증 규칙도 두 가지 더했습니다.

- **상속을 제품 고유 기능으로 오인하지 마십시오.** 포스트양자 심볼은 Chromium 포크라면 어디서나
  나옵니다. Chromium이 X25519MLKEM768 TLS를 기본 탑재하기 때문입니다. 제품 기능으로 귀속하기
  전에 파일 위치부터 갈라야 했습니다.
- **1차 출처끼리도 대조하십시오.** 한 벤더의 문서 색인과 제품 FAQ가 "계획이 로컬에서 도는가"에
  대해 서로 반대로 말했습니다. 더 구체적인 쪽을 채택하고, 모순 자체를 기록합니다.

Strands 조사가 하나를 더했습니다.

- **설계 문서의 상태 줄은 구현 상태가 아닙니다.** "Proposed"로 표시된 설계 13건 중 11건이 이미
  구현돼 있었고, 중요한 지점에서 설계와 달랐습니다. 코드 옆에 커밋된 설계는 권위 있어 보이지만,
  의도(📐)로 등급을 매기고 동작의 증거로 쓰지 않습니다.

세 항목은 의도적으로 열어 두었습니다. 정정된 2차 출처 오류의 기록, 추론임을 명시한 Windows 내부
구조, 그리고 클라이언트 카탈로그와는 다른 표면인 Agents API 서버 측 모델 목록입니다.
[99-sources.md](docs/99-sources.md)를 참고하십시오.

## 다음에 읽을 문서

| 관심 | 경로 |
|---|---|
| Codex 상세 | [`docs/`](docs/) — 문서 16편 |
| 브라우저형 에이전트 | [`browser-agents/README.md`](browser-agents/README.md) |
| 임베드형 SDK 하네스 (Strands) | [`strands/README.md`](strands/README.md) |
| **셋을 가로질러** | **[`SYNTHESIS.md`](SYNTHESIS.md)** |
| 개념 단위 | [`ai-workflow/wiki/index.md`](ai-workflow/wiki/index.md) — 개념 16종 |
| 어떤 주장을 믿어도 되나 | [`docs/99-sources.md`](docs/99-sources.md) · [`strands/99-sources.md`](strands/99-sources.md) |

---

내용은 2026-09-15 시점의 `openai/codex` 상태를 반영하며, 변화가 빠른 저장소입니다.
외부 증거는 2026-09-23에, Strands 사례는 2026-09-26에 추가했습니다 (`harness-sdk` @ `15da9dc`).
